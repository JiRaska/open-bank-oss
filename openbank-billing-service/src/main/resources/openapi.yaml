# SPDX-License-Identifier: Apache-2.0
# Copyright (c) OpenBank contributors. Licensed under the Apache License, Version 2.0.
# See LICENSE in the repository root or https://www.apache.org/licenses/LICENSE-2.0 for details.
openapi: 3.1.0
info:
  title: Billing Service API
  # major == openbank.api.version == URL /api/v1 (ADR-0048).
  version: 1.1.1
  description: >-
    Runtime product fee assessment and posting (ADR-0143). Phase 2c read path: POST
    /api/v1/fees/assess computes an account's fee assessment from live account/balance/catalog
    reads and returns it (dry run — no ledger posting yet; posting is phase 2c-ii).
servers:
  - url: http://localhost:8132
    description: Local development
paths:
  /api/v1/info:
    get:
      summary: Service identity and version (served by openbank-libs).
      responses:
        "200":
          description: Service info.
  /api/v1/fees/assess:
    post:
      summary: Assess (dry-run) an account's product fees for a billing cycle. Posts nothing.
      security:
        - bearerAuth: []
      parameters:
        - { name: cycleId, in: query, required: true, schema: { type: string } }
        - { name: accountId, in: query, required: true, schema: { type: string } }
        - { name: currency, in: query, required: true, schema: { type: string } }
      responses:
        "200":
          description: The computed assessment (assessed fees + chargeable journal commands).
        "400":
          description: Missing required query parameter.
        "401":
          description: Unauthenticated.
        "403":
          description: Authenticated but denied by the OPA authorization policy (ADR-0034).
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
