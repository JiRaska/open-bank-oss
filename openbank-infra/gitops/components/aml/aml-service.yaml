# aml-service: AML screening case lifecycle. Publishes openbank.aml.events via a
# transactional outbox (consumed by party-service for the KYC+AML activation gate,
# ADR-0073) and consumes openbank.party.events to auto-open onboarding screenings.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: aml-service
  namespace: aml
  labels:
    app.kubernetes.io/name: aml-service
    app.kubernetes.io/part-of: aml
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: aml-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: aml-service
        app.kubernetes.io/part-of: aml
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 100
        runAsGroup: 101
        fsGroup: 101
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: aml-service
          image: 265175468565.dkr.ecr.eu-north-1.amazonaws.com/openbank-aml-service:sandbox-31e6692a
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8117
            - name: management
              containerPort: 8085
          env:
            - name: QUARKUS_DATASOURCE_JDBC_URL
              value: jdbc:postgresql://aml-db-rw.aml.svc:5432/openbank_aml
            - name: QUARKUS_DATASOURCE_REACTIVE_URL
              value: vertx-reactive:postgresql://aml-db-rw.aml.svc:5432/openbank_aml
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: aml-db-app
                  key: password
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: openbank-cluster-kafka-bootstrap.messaging.svc:9093
            - name: QUARKUS_SMALLRYE_REACTIVE_MESSAGING_KAFKA_BOOTSTRAP_SERVERS
              value: openbank-cluster-kafka-bootstrap.messaging.svc:9093
            # mTLS to Strimzi (ADR-0137). Authenticates as KafkaUser 'aml-service';
            # keystore/truststore PKCS#12 are projected from messaging by External Secrets
            # (kafka-aml-mtls.yaml) and mounted below. The app reads these via ${...} in
            # application.yaml (mp.messaging.connector.smallrye-kafka.*).
            - name: KAFKA_SECURITY_PROTOCOL
              value: "SSL"
            - name: KAFKA_SSL_KEYSTORE_LOCATION
              value: /mnt/kafka/keystore/user.p12
            - name: KAFKA_SSL_KEYSTORE_TYPE
              value: PKCS12
            - name: KAFKA_SSL_KEYSTORE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: aml-service-kafka-keystore
                  key: user.password
            - name: KAFKA_SSL_TRUSTSTORE_LOCATION
              value: /mnt/kafka/truststore/ca.p12
            - name: KAFKA_SSL_TRUSTSTORE_TYPE
              value: PKCS12
            - name: KAFKA_SSL_TRUSTSTORE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: kafka-cluster-ca-truststore
                  key: ca.password
            - name: QUARKUS_HTTP_PORT
              value: "8117"
            - name: QUARKUS_OIDC_AUTH_SERVER_URL
              value: http://keycloak.iam.svc:8080/realms/openbank
            - name: OIDC_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: aml-service-oidc
                  key: OIDC_CLIENT_SECRET
            - name: QUARKUS_OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://tempo.observability.svc:4317"
            - name: OTEL_TRACES_SAMPLER_ARG
              value: "0.1"
            # Idempotency store (POST /aml/cases dedupes on Idempotency-Key) is Redis-backed.
            # Without this it defaults to localhost:6379 → connection refused → 500 on every
            # createCase. Point at the in-namespace redis (redis.yaml).
            - name: QUARKUS_REDIS_HOSTS
              value: redis://redis.aml.svc:6379
            # Sandbox straight-through AML (ADR-0073): auto-clear onboarding screenings so
            # onboarding activates without an analyst. MUST be false in production.
            - name: OPENBANK_AML_AUTO_CLEAR
              value: "true"
          startupProbe:
            tcpSocket:
              port: management
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 30
          readinessProbe:
            tcpSocket:
              port: http
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /q/health/live
              port: management
            periodSeconds: 15
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: kafka-keystore
              mountPath: /mnt/kafka/keystore
              readOnly: true
            - name: kafka-truststore
              mountPath: /mnt/kafka/truststore
              readOnly: true
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 512Mi
      volumes:
        - name: tmp
          emptyDir: {}
        - name: kafka-keystore
          secret:
            secretName: aml-service-kafka-keystore
        - name: kafka-truststore
          secret:
            secretName: kafka-cluster-ca-truststore
---
apiVersion: v1
kind: Service
metadata:
  name: aml-service
  namespace: aml
  labels:
    app.kubernetes.io/name: aml-service
    app.kubernetes.io/part-of: aml
spec:
  selector:
    app.kubernetes.io/name: aml-service
  ports:
    - name: http
      port: 8117
      targetPort: http
    - name: management
      port: 8085
      targetPort: management
