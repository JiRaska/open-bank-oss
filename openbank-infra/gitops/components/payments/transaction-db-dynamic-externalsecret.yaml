# ADR-0099 Tier 1 — dynamic DB credentials for transaction-service (Phase 1)
#
# ESO fetches short-lived Postgres credentials from OpenBao database secrets engine
# at database/creds/transaction-db-vault-role and projects them into `transaction-db-dynamic`.
# refreshInterval: 1h keeps the Secret ahead of the 24h lease TTL (max 72h).
# CNPG cluster: transaction-db, namespace: payments, DB: openbank_transactions
# Prerequisite: runbook 0006 §3 (vault_admin role + configure.sh run for transaction).
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: transaction-db-dynamic
  namespace: payments
  annotations:
    openbank.io/adr: "0099"
    openbank.io/rotation-tier: "1"
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-kv
  target:
    name: transaction-db-dynamic
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      engineVersion: v2
      mergePolicy: Replace
      metadata:
        annotations:
          argocd.argoproj.io/compare-options: IgnoreExtraneous
          secret.reloader.stakater.com/match: "true"
  data:
    - secretKey: username
      remoteRef:
        key: database/creds/transaction-db-vault-role
        property: username
    - secretKey: password
      remoteRef:
        key: database/creds/transaction-db-vault-role
        property: password
