# SPDX-License-Identifier: Apache-2.0
# Copyright (c) OpenBank contributors. Licensed under the Apache License, Version 2.0.
#
# DO NOT EDIT — generated by openbank-infra/scripts/gen-network-policies.py
# (ADR-0081: NetworkPolicy baseline derived from the declared GitOps edges).
#
# Pod-scoped ingress allow-lists: each rule admits only the namespaces whose
# Deployments declare a call to this service (env URLs), plus the static
# platform edges (same-namespace, Prometheus scrape on 8085, security-scanner
# posture probe on 8085, admin-ui BFF discovery, ingress-nginx where an Ingress
# backend exists, the keda namespace where an HTTPScaledObject fronts the
# workload — the KEDA HTTP add-on interceptor's proxied connection). CNPG Postgres
# pods are NOT selected (operator-managed, no Deployment) — intra-namespace
# data traffic is unaffected; Deployment-managed stores (e.g. redis) get a
# same-namespace-only policy, which is their entire caller set.
#
# ENFORCED: the VPC CNI network-policy agent runs in standard mode (ADR-0060),
# so this allow-list actively gates ingress — an undeclared caller is DROPPED.
# Rollback = delete the NetworkPolicy.
#
# SCOPE: this file holds the policies for the workloads declared in THIS component
# directory only, so the ArgoCD Application that owns the workload also owns the
# policy protecting it. A namespace shared by several component directories (e.g.
# `platform` = agent + ap2 + copilot + mcp) therefore has one such file per
# directory, not one for the whole namespace.
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: card-issuance-service-ingress-allow-list
  namespace: payments
  labels:
    app.kubernetes.io/part-of: payments
    openbank.io/derived: gen-network-policies
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: card-issuance-service
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector: {}
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: customer-edge
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: delegation
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: party
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: security-scanner
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: admin-ui
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: keda
      ports:
        - protocol: TCP
          port: 8118
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: observability
      ports:
        - protocol: TCP
          port: 8085
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: security-scanner
      ports:
        - protocol: TCP
          port: 8085
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: card-processing-service-ingress-allow-list
  namespace: payments
  labels:
    app.kubernetes.io/part-of: payments
    openbank.io/derived: gen-network-policies
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: card-processing-service
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector: {}
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: admin-ui
      ports:
        - protocol: TCP
          port: 8157
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: observability
      ports:
        - protocol: TCP
          port: 8085
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: security-scanner
      ports:
        - protocol: TCP
          port: 8085
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: clearing-service-ingress-allow-list
  namespace: payments
  labels:
    app.kubernetes.io/part-of: payments
    openbank.io/derived: gen-network-policies
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: clearing-service
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector: {}
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: platform
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: security-scanner
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: admin-ui
      ports:
        - protocol: TCP
          port: 8124
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: observability
      ports:
        - protocol: TCP
          port: 8085
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: security-scanner
      ports:
        - protocol: TCP
          port: 8085
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: clearing-simulator-ingress-allow-list
  namespace: payments
  labels:
    app.kubernetes.io/part-of: payments
    openbank.io/derived: gen-network-policies
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: clearing-simulator
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector: {}
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: admin-ui
      ports:
        - protocol: TCP
          port: 8139
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: observability
      ports:
        - protocol: TCP
          port: 8087
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: security-scanner
      ports:
        - protocol: TCP
          port: 8087
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: domestic-payment-ingress-allow-list
  namespace: payments
  labels:
    app.kubernetes.io/part-of: payments
    openbank.io/derived: gen-network-policies
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: domestic-payment
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector: {}
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: customer-edge
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: security-scanner
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: admin-ui
      ports:
        - protocol: TCP
          port: 8116
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: observability
      ports:
        - protocol: TCP
          port: 8085
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: security-scanner
      ports:
        - protocol: TCP
          port: 8085
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: ingress-nginx
      ports:
        - protocol: TCP
          port: 8116
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: redis-ingress-allow-list
  namespace: payments
  labels:
    app.kubernetes.io/part-of: payments
    openbank.io/derived: gen-network-policies
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: redis
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector: {}
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: admin-ui
      ports:
        - protocol: TCP
          port: 9121
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: observability
      ports:
        - protocol: TCP
          port: 9121
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: sepa-instant-ingress-allow-list
  namespace: payments
  labels:
    app.kubernetes.io/part-of: payments
    openbank.io/derived: gen-network-policies
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: sepa-instant
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector: {}
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: customer-edge
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: platform
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: security-scanner
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: admin-ui
      ports:
        - protocol: TCP
          port: 8127
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: observability
      ports:
        - protocol: TCP
          port: 8085
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: security-scanner
      ports:
        - protocol: TCP
          port: 8085
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: ingress-nginx
      ports:
        - protocol: TCP
          port: 8127
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: sepa-payment-ingress-allow-list
  namespace: payments
  labels:
    app.kubernetes.io/part-of: payments
    openbank.io/derived: gen-network-policies
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: sepa-payment
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector: {}
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: customer-edge
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: security-scanner
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: admin-ui
      ports:
        - protocol: TCP
          port: 8115
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: observability
      ports:
        - protocol: TCP
          port: 8085
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: security-scanner
      ports:
        - protocol: TCP
          port: 8085
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: ingress-nginx
      ports:
        - protocol: TCP
          port: 8115
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: settlement-service-ingress-allow-list
  namespace: payments
  labels:
    app.kubernetes.io/part-of: payments
    openbank.io/derived: gen-network-policies
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: settlement-service
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector: {}
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: admin-ui
      ports:
        - protocol: TCP
          port: 8138
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: observability
      ports:
        - protocol: TCP
          port: 8085
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: security-scanner
      ports:
        - protocol: TCP
          port: 8085
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: standing-order-service-ingress-allow-list
  namespace: payments
  labels:
    app.kubernetes.io/part-of: payments
    openbank.io/derived: gen-network-policies
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: standing-order-service
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector: {}
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: customer-edge
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: security-scanner
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: admin-ui
      ports:
        - protocol: TCP
          port: 8121
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: observability
      ports:
        - protocol: TCP
          port: 8085
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: security-scanner
      ports:
        - protocol: TCP
          port: 8085
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: swift-service-ingress-allow-list
  namespace: payments
  labels:
    app.kubernetes.io/part-of: payments
    openbank.io/derived: gen-network-policies
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: swift-service
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector: {}
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: customer-edge
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: security-scanner
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: admin-ui
      ports:
        - protocol: TCP
          port: 8122
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: observability
      ports:
        - protocol: TCP
          port: 8085
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: security-scanner
      ports:
        - protocol: TCP
          port: 8085
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: transaction-service-ingress-allow-list
  namespace: payments
  labels:
    app.kubernetes.io/part-of: payments
    openbank.io/derived: gen-network-policies
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: transaction-service
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector: {}
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: accounts
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: customer-edge
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: lending
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: platform
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: sdd
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: security-scanner
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: statements
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: admin-ui
      ports:
        - protocol: TCP
          port: 8102
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: observability
      ports:
        - protocol: TCP
          port: 8085
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: security-scanner
      ports:
        - protocol: TCP
          port: 8085
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: ingress-nginx
      ports:
        - protocol: TCP
          port: 8102
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: vop-service-ingress-allow-list
  namespace: payments
  labels:
    app.kubernetes.io/part-of: payments
    openbank.io/derived: gen-network-policies
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: vop-service
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector: {}
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: customer-edge
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: admin-ui
      ports:
        - protocol: TCP
          port: 8149
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: observability
      ports:
        - protocol: TCP
          port: 8086
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: security-scanner
      ports:
        - protocol: TCP
          port: 8086
