# SPDX-License-Identifier: Apache-2.0
# Copyright (c) OpenBank contributors. Licensed under the Apache License, Version 2.0.
#
# Private-CA server certificate for consent-service's east-west mTLS listener (8443). The existing HTTP
# listener remains during the east-west TLS migration; new consumers use the parallel 8443 listener.
# Same shape as account-service-internal-tls and document-service-internal-tls.
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: consent-service-internal-tls
  namespace: consent
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  secretName: consent-service-internal-tls
  duration: 2160h
  renewBefore: 360h
  privateKey:
    algorithm: ECDSA
    size: 256
  dnsNames:
    - consent-service.consent.svc
    - consent-service.consent.svc.cluster.local
  issuerRef:
    name: openbank-ca
    kind: ClusterIssuer
    group: cert-manager.io
