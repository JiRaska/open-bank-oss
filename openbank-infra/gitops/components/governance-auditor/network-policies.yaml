# SPDX-License-Identifier: Apache-2.0
# Copyright (c) OpenBank contributors. Licensed under the Apache License, Version 2.0.
#
# DO NOT EDIT — generated by openbank-infra/scripts/gen-network-policies.py
# (ADR-0081: NetworkPolicy baseline derived from the declared GitOps edges).
#
# Pod-scoped ingress allow-lists: each rule admits only the namespaces whose Deployments declare
# a call to this service (env URLs), plus the static platform edges (same-namespace, Prometheus
# scrape on 8085, security-scanner posture probe on 8085, admin-ui BFF discovery).
#
# ENFORCED: the VPC CNI network-policy agent runs in standard mode (ADR-0060), so this allow-list
# actively gates ingress — an undeclared caller is DROPPED. Rollback = delete the NetworkPolicy.
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: governance-auditor-ingress-allow-list
  namespace: governance-auditor
  labels:
    app.kubernetes.io/part-of: governance-auditor
    openbank.io/derived: gen-network-policies
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: governance-auditor
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector: {}
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: admin-ui
      ports:
        - protocol: TCP
          port: 8143
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: observability
      ports:
        - protocol: TCP
          port: 8085
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: security-scanner
      ports:
        - protocol: TCP
          port: 8085
