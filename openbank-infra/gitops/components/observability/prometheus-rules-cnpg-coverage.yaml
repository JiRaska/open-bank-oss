# GENERATED by .github/scripts/check-cnpg-scrape-coverage.py -- DO NOT EDIT.
# Regenerate with: python3 .github/scripts/check-cnpg-scrape-coverage.py --write
#
# Absence as a STATE, not a silence.
#
# Every Postgres alert in prometheus-rules-db.yaml is a threshold over a series the cluster must
# emit -- `up` included, since Prometheus writes `up` per TARGET and a Cluster with no PodMonitor
# is not a target. So a cluster nobody scrapes produces no metric, no alert and no absence signal:
# the expression matches nothing, which is indistinguishable from healthy.
#
# The fix is a denominator that exists whether or not the cluster does. The recording rules below
# are constant `vector(1)` series -- one per CNPG Cluster DERIVED FROM THE MANIFESTS in this repo,
# regenerated by the gate above, never hand-kept -- so `expected unless scraped` is a real vector
# when a cluster goes missing from Prometheus.
#
# Picked up automatically (ruleSelectorNilUsesHelmValues=false), like every sibling rule file.

apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: openbank-cnpg-scrape-coverage
  namespace: observability
  labels:
    app.kubernetes.io/part-of: observability
spec:
  groups:
    - name: openbank.db.cnpg-coverage
      rules:
        # The DENOMINATOR. One constant series per CNPG Cluster declared in this repo.
        # Derived from the manifests, so it cannot be shorter than reality.
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: accounts
            cluster: accounts-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: accounts
            cluster: product-catalog-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: ai-platform
            cluster: langfuse-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: ai-platform
            cluster: litellm-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: aml
            cluster: aml-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: anacredit
            cluster: anacredit-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: audit
            cluster: audit-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: authz-policy-auditor
            cluster: authzaudit-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: balances
            cluster: balances-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: billing
            cluster: billing-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: campaign
            cluster: campaign-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: consent
            cluster: consent-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: control-liveness-sentinel
            cluster: liveness-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: delegation
            cluster: delegation-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: devops-agent
            cluster: devops-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: dispute
            cluster: dispute-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: docs-truth-agent
            cluster: docstruth-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: documents
            cluster: document-service-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: engagement
            cluster: engagement-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: finops-agent
            cluster: finops-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: flaky-test-hunter
            cluster: flakytest-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: fraud
            cluster: fraud-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: fx
            cluster: fx-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: governance-auditor
            cluster: govaudit-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: iam
            cluster: keycloak-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: incentive
            cluster: incentive-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: interest
            cluster: interest-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: kyb
            cluster: kyb-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: kyc
            cluster: kyc-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: ledger
            cluster: ledger-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: lending
            cluster: lending-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: messaging
            cluster: apicurio-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: notifications
            cluster: notifications-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: observability
            cluster: glitchtip-pg
            origin: chart
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: observability
            cluster: goalert-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: onboarding
            cluster: onboarding-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: pact-broker
            cluster: pact-broker-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: party
            cluster: party-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: payments
            cluster: card-issuance-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: payments
            cluster: clearing-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: payments
            cluster: domestic-payment-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: payments
            cluster: sepa-instant-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: payments
            cluster: sepa-payment-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: payments
            cluster: settlement-service-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: payments
            cluster: standing-order-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: payments
            cluster: swift-service-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: payments
            cluster: transaction-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: payments
            cluster: vop-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: pid
            cluster: pid-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: platform
            cluster: agent-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: platform
            cluster: case-coordinator-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: platform
            cluster: copilot-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: platform
            cluster: mcp-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: psd2
            cluster: psd2-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: referral
            cluster: referral-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: release-steward
            cluster: releasesteward-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: sanctions
            cluster: sanctions-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: sca
            cluster: sca-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: sdd
            cluster: sdd-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: security-scanner
            cluster: security-scanner-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: statements
            cluster: statements-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: temporal
            cluster: temporal-db
            origin: manifest
        - record: openbank:cnpg_cluster_expected
          expr: vector(1)
          labels:
            namespace: tpp-registry
            cluster: tpp-registry-db
            origin: manifest

        # The NUMERATOR, from a series Prometheus writes itself. `up` exists for every
        # target, 0 when the scrape fails -- so a cluster that is down still appears here
        # and is NOT reported as unscraped. Only a cluster with no target at all vanishes.
        - record: openbank:cnpg_cluster_scraped
          expr: |
            max by (namespace, cluster) (
              label_replace(up{container="postgres"}, "cluster", "$1", "pod", "(.+)-[0-9]+")
            )

        - alert: PostgresClusterUnscraped
          # Fires when a cluster this repo declares has NO scrape target in Prometheus.
          # `unless` is the whole alert: it yields the expected series that has no
          # counterpart, which is a real vector -- where the sibling threshold alerts
          # yield an empty one and stay silent forever.
          #
          # 15m absorbs a rollout: a Cluster being recreated loses its target briefly.
          expr: |
            openbank:cnpg_cluster_expected
              unless on (namespace, cluster) openbank:cnpg_cluster_scraped
          for: 15m
          labels:
            severity: critical
          annotations:
            summary: "CNPG cluster {{ $labels.namespace }}/{{ $labels.cluster }} is declared but NOT scraped"
            description: "This cluster is declared in gitops and Prometheus has no scrape target for it, so every Postgres alert -- PostgresNoRecoveryPoint, PostgresBackupStale, PostgresWALArchiveFailing and even PostgresInstanceDown -- is structurally incapable of firing for it. Its backup state is unknown, not healthy. Set spec.monitoring.enablePodMonitor: true on the Cluster, or declare it in NOT_SCRAPED in .github/scripts/check-cnpg-scrape-coverage.py with a reason."
