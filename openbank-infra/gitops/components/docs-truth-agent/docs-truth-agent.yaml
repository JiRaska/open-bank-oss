apiVersion: apps/v1
kind: Deployment
metadata:
  name: docs-truth-agent
  namespace: docs-truth-agent
  labels:
    app.kubernetes.io/name: docs-truth-agent
    app.kubernetes.io/part-of: docs-truth-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: docs-truth-agent
  template:
    metadata:
      labels:
        app.kubernetes.io/name: docs-truth-agent
        app.kubernetes.io/part-of: docs-truth-agent
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 100
        runAsGroup: 101
        fsGroup: 101
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: docs-truth-agent
          # Pending first CI build (ADR-0166 initial PR) — build-push-service.sh publishes the
          # real sandbox-<sha> tag on merge; ArgoCD will not sync a healthy pod until then.
          image: 265175468565.dkr.ecr.eu-north-1.amazonaws.com/openbank-docs-truth-agent:sandbox-a499cad1
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8146
            - name: management
              containerPort: 8085
          env:
            - name: QUARKUS_OIDC_AUTH_SERVER_URL
              value: http://keycloak.iam.svc:8080/realms/openbank
            - name: QUARKUS_DATASOURCE_REACTIVE_URL
              value: postgresql://docstruth-db-rw.docs-truth-agent.svc:5432/openbank_docstruth
            - name: QUARKUS_DATASOURCE_JDBC_URL
              value: jdbc:postgresql://docstruth-db-rw.docs-truth-agent.svc:5432/openbank_docstruth
            - name: QUARKUS_DATASOURCE_USERNAME
              valueFrom: {secretKeyRef: {name: docstruth-db-app, key: username}}
            - name: QUARKUS_DATASOURCE_PASSWORD
              valueFrom: {secretKeyRef: {name: docstruth-db-app, key: password}}
            - name: TEMPORAL_HOST
              value: temporal-frontend.temporal.svc:7233
            - name: TEMPORAL_ENABLED
              value: "true"
            - name: GITHUB_API_URL
              value: https://api.github.com
            - name: GITHUB_REPO
              value: JiRaska/open-bank-oss
            - name: LLM_GATEWAY_URL
              value: http://litellm.ai-platform.svc:4000
            - name: TEMPORAL_NAMESPACE
              value: openbank-docstruth
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /q/health/ready
              port: management
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /q/health/live
              port: management
            initialDelaySeconds: 20
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: docs-truth-agent
  namespace: docs-truth-agent
  labels:
    app.kubernetes.io/name: docs-truth-agent
    app.kubernetes.io/part-of: docs-truth-agent
spec:
  selector:
    app.kubernetes.io/name: docs-truth-agent
  ports:
    - name: http
      port: 8146
      targetPort: http
    - name: management
      port: 8085
      targetPort: management
