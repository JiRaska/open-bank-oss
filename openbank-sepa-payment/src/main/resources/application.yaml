quarkus:
  application:
    name: openbank-sepa-payment
  http:
    port: 8115
    cors:
      ~: true
      origins: "http://localhost:3000"
      methods: GET,POST,PUT,PATCH,DELETE,OPTIONS
      headers: Content-Type,Authorization,Idempotency-Key
    header:
      X-Content-Type-Options:
        value: nosniff
      X-Frame-Options:
        value: DENY
      X-XSS-Protection:
        value: "1; mode=block"
      Referrer-Policy:
        value: strict-origin-when-cross-origin
      Permissions-Policy:
        value: "geolocation=(), microphone=(), camera=()"
      Content-Security-Policy:
        value: "default-src 'self'"
      Strict-Transport-Security:
        value: "max-age=31536000; includeSubDomains"

  datasource:
    db-kind: postgresql
    username: openbank
    password: ${POSTGRES_PASSWORD:CHANGE_ME_LOCAL_DEV_ONLY}
    reactive:
      url: postgresql://localhost:5432/openbank_sepa_payments
    jdbc:
      url: jdbc:postgresql://localhost:5432/openbank_sepa_payments
  flyway:
    migrate-at-start: true
    connect-retries: 10
    connect-retries-interval: 2S
  hibernate-orm:
    database:
      generation: none
  oidc:
    auth-server-url: http://localhost:8080/realms/openbank
    client-id: openbank-services
    credentials:
      secret: ${OIDC_CLIENT_SECRET:CHANGE_ME_LOCAL_DEV_ONLY}
    tls:
      verification: none
  # Outbound service-to-service token (ADR-0104 D3 / Bug #3): the rest-clients (sanctions, aml,
  # fraud, clearing-simulator) mint an openbank-services M2M token via this oidc-client. Without
  # it the default client is "not initialized" → no Authorization header → downstream 401 (the
  # scheme submission held every payment in VALIDATED). Reuses the resource-server's keycloak URL,
  # client id and secret. Disabled under %test; the IT enables + stubs it, units use a producer.
  oidc-client:
    auth-server-url: ${QUARKUS_OIDC_AUTH_SERVER_URL:http://localhost:8080/realms/openbank}
    client-id: openbank-services
    credentials:
      secret: ${OIDC_CLIENT_SECRET:CHANGE_ME_LOCAL_DEV_ONLY}
    grant:
      type: client
  redis:
    hosts: redis://localhost:6379
  rest-client:
    sanctions-service:
      url: ${SANCTIONS_SERVICE_URL:http://localhost:8123}
      scope: jakarta.inject.Singleton
    aml-service:
      url: ${AML_SERVICE_URL:http://localhost:8117}
      scope: jakarta.inject.Singleton
    fraud-service:
      url: ${FRAUD_SERVICE_URL:http://localhost:8133}
      scope: jakarta.inject.Singleton
    clearing-simulator:
      url: ${CLEARING_SIMULATOR_URL:http://localhost:8139}
      scope: jakarta.inject.Singleton
    transaction-service:
      url: ${TRANSACTION_SERVICE_URL:http://localhost:8102}
      scope: jakarta.inject.Singleton
    # #8505: resolves debtorAccountId → owning party for the AML case's party_id (the SEPA
    # rail's port of the domestic #3274 fix). Read by AccountServiceClient via @ConfigProperty.
    account-service:
      url: ${ACCOUNT_SERVICE_URL:http://localhost:8100}
      scope: jakarta.inject.Singleton
    document-service:
      url: ${DOCUMENT_SERVICE_URL:http://localhost:8143}
      scope: jakarta.inject.Singleton
  smallrye-reactive-messaging:
    kafka:
      bootstrap-servers: "${KAFKA_BOOTSTRAP_SERVERS:localhost:29092}"
  shutdown:
    timeout: 30S
  log:
    level: INFO
    console:
      json: true
  otel:
    exporter:
      otlp:
        endpoint: http://localhost:4317
    resource:
      attributes:
        service.name: openbank-sepa-payment
  swagger-ui:
    always-include: true
    path: /api/docs
  management:
    enabled: true
    port: 8085
    host: 0.0.0.0
    root-path: /q
  dev-ui:
    enabled: false

mp:
  messaging:
    # mTLS to Strimzi (ADR-0137). These connector-wide Kafka client props
    # apply to every smallrye-kafka channel. Defaults keep local dev/test on
    # PLAINTEXT (empty keystore paths are ignored when protocol=PLAINTEXT);
    # the payments Rollout sets the KAFKA_* env in prod to switch to mTLS.
    connector:
      smallrye-kafka:
        security.protocol: ${KAFKA_SECURITY_PROTOCOL:PLAINTEXT}
        ssl.keystore.location: ${KAFKA_SSL_KEYSTORE_LOCATION:}
        ssl.keystore.type: ${KAFKA_SSL_KEYSTORE_TYPE:PKCS12}
        ssl.keystore.password: ${KAFKA_SSL_KEYSTORE_PASSWORD:}
        ssl.truststore.location: ${KAFKA_SSL_TRUSTSTORE_LOCATION:}
        ssl.truststore.type: ${KAFKA_SSL_TRUSTSTORE_TYPE:PKCS12}
        ssl.truststore.password: ${KAFKA_SSL_TRUSTSTORE_PASSWORD:}
    outgoing:
      events-out:
        connector: smallrye-kafka
        client.id: sepa-payment-${quarkus.uuid}
        topic: openbank.sepa.payment.events
        value.serializer: org.apache.kafka.common.serialization.StringSerializer
"%dev":
  quarkus:
    log:
      level: DEBUG
      console:
        json: false
    oidc:
      enabled: false
"%test":
  quarkus:
    oidc:
      enabled: false
    oidc-client:
      enabled: false
    management:
      enabled: false
    devservices:
      enabled: false
  openbank:
    sepa:
      # No real Temporal frontend in @QuarkusTest: don't register the worker (it would fail boot on
      # UNAVAILABLE). Tests that need the workflow drive their own in-process TestWorkflowEnvironment.
      worker:
        enabled: false
openbank:
  temporal:
    server-url: localhost:7233
    namespace: openbank-payments
    task-queue: openbank-sepa-payment
  bank:
    # OpenBank's own BIC — used as the debtor agent (DbtrAgt) in the pacs.008 (ADR-0104 D3).
    bic: ${OPENBANK_BIC:OPBKCZ22}
  sepa:
    scheme-submission:
      # ADR-0104 D3 pilot. OFF by default: when off, behaviour is exactly as before (screening only).
      enabled: ${SEPA_SCHEME_SUBMISSION_ENABLED:false}
  outbox:
    dispatch-enabled: true
    poll-interval: 5s
    initial-delay: 5s
  rate-limit:
    enabled: true
    max-concurrent-requests: 100
  resilience:
    circuit-breaker:
      enabled: true
      request-volume-threshold: 15
      failure-ratio: 0.5
      success-threshold: 5
      delay-ms: 10000
    retry:
      enabled: true
      max-retries: 3
      delay-ms: 500
      jitter-ms: 200
    timeout:
      enabled: true
      value-ms: 30000
  api:
    version: "1"
  build:
    time: "${BUILD_TIME:unknown}"
  git:
    commit: "${GIT_COMMIT:unknown}"

# OPA sidecar (ADR-0034). Advisory mode by default.
opa:
  url: "${OPA_URL:http://localhost:8181}"
  path: "${OPA_PATH:/v1/data/openbank/rest/allow}"
  timeout-ms: "${OPA_TIMEOUT_MS:500}"
authz:
  enforce: "${AUTHZ_ENFORCE:false}"
  # ADR-0155: four-eyes ENFORCEMENT (blocking a maker's request pending a second
  # approver), independent of the base allow/deny enforce toggle above. Stays false
  # here even though rest.rego now correctly computes four_eyes_required for
  # sepaPayment.transitionStatus (issue #395) — flipping this is a deliberate,
  # separate follow-up once the maker/checker runbook is reviewed, not bundled with
  # the ApprovalStore wiring itself.
  four-eyes:
    enforce: "${AUTHZ_FOUR_EYES_ENFORCE:false}"
