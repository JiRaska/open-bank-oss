# Same workaround as the root build: Kotlin 2.0.x emits max JVM-22 bytecode while
# javac/toolchain run on 25 — downgrade the target-validation error to a warning.
kotlin.jvm.target.validation.mode=warning
