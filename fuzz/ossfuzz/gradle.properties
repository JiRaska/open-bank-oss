# Same workaround as the root build: the Kotlin compiler emits a lower max JVM bytecode
# target than the javac/toolchain JDK (25), which Gradle flags as an inconsistency —
# downgrade that target-validation error to a warning.
kotlin.jvm.target.validation.mode=warning
