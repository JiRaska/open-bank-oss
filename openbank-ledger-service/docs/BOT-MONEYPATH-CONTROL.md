# Bot identity smoke test — money path (must FAIL)

Opened by the `openbank-agent-bot` App to prove three things nothing had yet verified:
the App can push a branch, it can open a pull request, and `agent-pr-guard` classifies it
from the AUTHOR (this branch deliberately does **not** use the `agent/` prefix).

Closed as soon as the verdict is recorded. Never merged.
