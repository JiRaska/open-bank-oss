quarkus:
  application:
    name: devops-agent
  http:
    port: 8142
    cors:
      ~: true
      origins: "http://localhost:3000"
      methods: GET,POST,PUT,PATCH,DELETE,OPTIONS
      headers: Content-Type,Authorization
    header:
      X-Content-Type-Options:
        value: nosniff
      X-Frame-Options:
        value: DENY
      X-XSS-Protection:
        value: "1; mode=block"
      Referrer-Policy:
        value: strict-origin-when-cross-origin
      Permissions-Policy:
        value: "geolocation=(), microphone=(), camera=()"
      Content-Security-Policy:
        value: "default-src 'self'"
      Strict-Transport-Security:
        value: "max-age=31536000; includeSubDomains"
  oidc:
    auth-server-url: http://localhost:8080/realms/openbank
    client-id: openbank-services
    credentials:
      secret: ${OIDC_CLIENT_SECRET:CHANGE_ME_LOCAL_DEV_ONLY}
    tls:
      verification: none
  datasource:
    db-kind: postgresql
    username: ${POSTGRES_USER:openbank}
    password: ${POSTGRES_PASSWORD:CHANGE_ME_LOCAL_DEV_ONLY}
    # Reactive client for the app (Panache); JDBC URL is for Flyway migrations only.
    reactive:
      url: ${REACTIVE_URL:postgresql://localhost:5432/openbank_devops}
    jdbc:
      url: ${JDBC_URL:jdbc:postgresql://localhost:5432/openbank_devops}
  hibernate-orm:
    # Flyway owns the schema; Hibernate validates against it, never generates.
    database:
      generation: none
  flyway:
    migrate-at-start: true
  shutdown:
    timeout: 30S
  log:
    level: INFO
  otel:
    exporter:
      otlp:
        endpoint: http://localhost:4317
  swagger-ui:
    always-include: true
    path: /api/docs
  management:
    enabled: true
    port: 8085
    host: 0.0.0.0
    root-path: /q
  dev-ui:
    enabled: false

"%dev":
  quarkus:
    log:
      level: DEBUG
    oidc:
      enabled: false

"%test":
  quarkus:
    oidc:
      enabled: false
    management:
      enabled: false
    http:
      # Ephemeral: this module now has more than one @QuarkusTest profile, so there is more than
      # one application boot per run, and on the fixed default the second dies with
      # QuarkusBindException whenever anything else on the machine holds the port.
      test-port: 0

  openbank:
    devops:
      # A cron that never fires (the 31st of February), NOT `quarkus.scheduler.enabled: false`.
      # Disabling the scheduler makes the class structurally invisible to every test — which is how
      # five schedulers in this fleet reached production having never run (#2148, #2187). With a
      # real-but-unreachable expression the wiring is still built and validated at boot, and a test
      # profile can override this key with a fast cron to drive the REAL schedule.
      analysis-cron: "0 0 5 31 2 ?"

openbank:
  temporal:
    enabled: ${TEMPORAL_ENABLED:false}
    server-url: ${TEMPORAL_HOST:temporal-frontend.temporal:7233}
    namespace: ${TEMPORAL_NAMESPACE:openbank}
    task-queue: devops-agent
    # ADR-0209 D1 (#2572): this service's now-deleted TemporalClientProducer copy was the ONE of the
    # 14 that built its service stubs with no Micrometer metrics scope. Extracting to the shared
    # producer would have silently started emitting Temporal gRPC client metrics here, so the
    # divergence is preserved as config rather than flattened. Flip to true to adopt the fleet norm.
    metrics-enabled: false
  devops:
    # Daily analysis sweep (ADR-0119). 03:30 UTC — the three AI agents are offset from each
    # other by 15 minutes because they share ONE LiteLLM gateway and one Prometheus, and each now
    # carries its own daily token budget: finops-agent (03:00) and the control-liveness sentinel (03:15).
    # Collisions with unrelated per-service batch jobs are not a consideration — different pods,
    # different databases. Quarkus cron is SIX fields (second minute hour day month weekday).
    analysis-cron: ${DEVOPS_ANALYSIS_CRON:0 30 3 * * ?}
    prometheus-url: ${PROMETHEUS_URL:http://prometheus-operated.observability:9090}
    alertmanager-url: ${ALERTMANAGER_URL:http://alertmanager-operated.observability:9093}
    # OpenAI-compatible model backend — DeepInfra + DeepSeek, same provider+model as the copilot
    # (ADR-0089). The API key is read separately (devops.model.api-key ← DEVOPS_MODEL_API_KEY) so an
    # un-seeded key degrades the diagnosis instead of CrashLooping the pod.
    model-endpoint: ${DEVOPS_MODEL_ENDPOINT:https://api.deepinfra.com/v1/openai}
    model-id: ${DEVOPS_MODEL_ID:deepseek-ai/DeepSeek-V3.2}
    ci-failure-rate-threshold: ${CI_FAILURE_RATE_THRESHOLD:0.20}
    change-failure-rate-threshold: ${CHANGE_FAILURE_RATE_THRESHOLD:0.05}
    runner-queue-pressure-threshold: ${RUNNER_QUEUE_PRESSURE_THRESHOLD:0.80}
    ssdlc-drift-threshold: ${SSDLC_DRIFT_THRESHOLD:3}
    incident-recurrence-threshold: ${INCIDENT_RECURRENCE_THRESHOLD:3}
