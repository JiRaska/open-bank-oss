<?xml version="1.0" encoding="utf-8"?>
<baseline version="1.0">
    <!--
      ONE entry, and it is unavoidable rather than debt.

      `in` is a Kotlin hard keyword, so the hexagonal inbound-port package (ADR-0002) has to be
      written `port.`in`` and ktlint's standard:package-name rule objects to the backticks. Every
      module in the fleet carries the same baselined entry for the same reason; the alternative is
      renaming the layer in one service only, which would make this the odd module out for a lint
      rule rather than for anything a reader cares about.

      Nothing else is baselined here, deliberately: this module was written against the current
      rule set, so every other finding was fixed rather than frozen.
    -->
    <file name="src/main/kotlin/com/openbank/cardprocessing/application/port/in/CardProcessingUseCases.kt">
        <error line="7" column="9" source="standard:package-name" />
    </file>
</baseline>
