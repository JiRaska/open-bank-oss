quarkus:
  application:
    name: control-liveness-sentinel
  http:
    port: 8142
    cors:
      ~: true
      origins: "http://localhost:3000"
      methods: GET,POST,PUT,PATCH,DELETE,OPTIONS
      headers: Content-Type,Authorization
    header:
      X-Content-Type-Options:
        value: nosniff
      X-Frame-Options:
        value: DENY
      X-XSS-Protection:
        value: "1; mode=block"
      Referrer-Policy:
        value: strict-origin-when-cross-origin
      Permissions-Policy:
        value: "geolocation=(), microphone=(), camera=()"
      Content-Security-Policy:
        value: "default-src 'self'"
      Strict-Transport-Security:
        value: "max-age=31536000; includeSubDomains"
  oidc:
    auth-server-url: http://localhost:8080/realms/openbank
    client-id: openbank-services
    credentials:
      secret: ${OIDC_CLIENT_SECRET:CHANGE_ME_LOCAL_DEV_ONLY}
    tls:
      verification: none
  shutdown:
    timeout: 30S
  log:
    level: INFO
  otel:
    exporter:
      otlp:
        endpoint: http://localhost:4317
  swagger-ui:
    always-include: true
    path: /api/docs
  management:
    enabled: true
    port: 8085
    host: 0.0.0.0
    root-path: /q
  dev-ui:
    enabled: false

"%dev":
  quarkus:
    log:
      level: DEBUG
    oidc:
      enabled: false

"%test":
  quarkus:
    oidc:
      enabled: false
    management:
      enabled: false

openbank:
  temporal:
    enabled: ${TEMPORAL_ENABLED:false}
    server-url: ${TEMPORAL_HOST:temporal-frontend.temporal:7233}
    namespace: ${TEMPORAL_NAMESPACE:openbank}
    task-queue: control-liveness-sentinel
  liveness-sentinel:
    prometheus-url: ${PROMETHEUS_URL:http://prometheus-operated.observability:9090}
    alertmanager-url: ${ALERTMANAGER_URL:http://alertmanager-operated.observability:9093}
    llm-gateway-url: ${LLM_GATEWAY_URL:http://litellm.ai-platform:4000}
    # 2x expected interval == the same paging threshold ADR-0160 mechanism 3 defines, so this
    # agent's WARNING/CRITICAL split can never silently disagree with the Alertmanager rule.
    stale-heartbeat-multiplier: ${STALE_HEARTBEAT_MULTIPLIER:2.0}
    consecutive-drift-threshold: ${CONSECUTIVE_DRIFT_THRESHOLD:3}
