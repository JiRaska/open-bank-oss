# SPDX-License-Identifier: MPL-2.0
# Copyright (c) OpenBank contributors. Licensed under the Mozilla Public License 2.0.
openapi: 3.1.0
info:
  title: Standing Order Service API
  version: 1.0.0
  description: Recurring payment orders — create, pause, resume, cancel.
  license:
    name: MPL-2.0
    url: https://www.mozilla.org/MPL/2.0/
servers:
  - url: http://localhost:8116
    description: Local development
tags:
  - name: Standing Orders

paths:
  /api/v1/standing-orders:
    post:
      tags: [Standing Orders]
      summary: Create a standing order
      operationId: createStandingOrder
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateStandingOrderRequest'
      responses:
        '201':
          description: Standing order created

  /api/v1/standing-orders/{id}:
    get:
      tags: [Standing Orders]
      summary: Get standing order by ID
      operationId: getStandingOrder
      parameters:
        - $ref: '#/components/parameters/orderId'
      responses:
        '200':
          description: Standing order detail
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      tags: [Standing Orders]
      summary: Cancel a standing order
      operationId: cancelStandingOrder
      parameters:
        - $ref: '#/components/parameters/orderId'
      responses:
        '204':
          description: Cancelled

  /api/v1/standing-orders/party/{partyId}:
    get:
      tags: [Standing Orders]
      summary: List standing orders for a party
      operationId: listStandingOrdersByParty
      parameters:
        - name: partyId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Standing orders

  /api/v1/standing-orders/{id}/pause:
    post:
      tags: [Standing Orders]
      summary: Pause a standing order
      operationId: pauseStandingOrder
      parameters:
        - $ref: '#/components/parameters/orderId'
      responses:
        '200':
          description: Paused

  /api/v1/standing-orders/{id}/resume:
    post:
      tags: [Standing Orders]
      summary: Resume a paused standing order
      operationId: resumeStandingOrder
      parameters:
        - $ref: '#/components/parameters/orderId'
      responses:
        '200':
          description: Resumed

components:
  parameters:
    orderId:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid

  responses:
    NotFound:
      description: Not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiError'

  schemas:
    CreateStandingOrderRequest:
      type: object
      required: [debtorAccountId, creditorIban, creditorName, amount, currencyCode, frequency, startDate]
      properties:
        debtorAccountId:
          type: string
          format: uuid
        creditorIban:
          type: string
        creditorName:
          type: string
        amount:
          type: number
        currencyCode:
          type: string
        frequency:
          type: string
          enum: [DAILY, WEEKLY, BIWEEKLY, MONTHLY, QUARTERLY, ANNUALLY]
        startDate:
          type: string
          format: date
        endDate:
          type: string
          format: date
          nullable: true
        remittanceInfo:
          type: string

    ApiError:
      type: object
      properties:
        code:
          type: string
        message:
          type: string
